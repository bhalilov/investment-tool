Reconstruct visible social-media threads from a set of manual screenshots.

First, logically stitch screenshots into scroll groups by matching overlap, repeated posts, visible reply chains, timestamps, and root posts.

A screenshot set may contain more than one thread. Create separate reconstructed threads when roots or reply contexts differ.

Merge duplicate/overlapping posts, but keep screenshot indexes as evidence.

Preserve only visible text. Do not invent missing cut-off text. Mark cut-off starts/ends.

Images embedded inside visible posts are embedded media; describe visible content on the post.

Do not infer investment signal, priority, portfolio action, or correctness. This pass only reconstructs source evidence.

Use the configured source profile to identify tracked source accounts and aliases.
